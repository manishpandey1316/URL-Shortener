<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <style>
      html,
      body {
        margin: 0;
        padding: 0;
      }
      body {
        background-color: cyan;
        display: flex;
        justify-content: center;
      }
      .container {
        background-color: white;
        display: flex;
        flex-direction: column;
        align-items: center;
        height: 60vh;
        width: 50vw;
        margin-top: 5rem;
        border: 2px grey;
        border-radius: 10px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
        padding: 2rem;
        gap: 1rem;
      }
      h1{
        color:rgb(21, 21, 128);
      }
      form {
        display: flex;
        flex-direction: column;
        align-items:center;
        gap:2rem;
      }
      label {
        font-size: 1.2rem;
        font-weight: bold;
      }
      .section {
        display: flex;
        gap:1rem;
      }
      input{
         width:20rem;
         box-shadow:0 0 5px black;
        
      }
      button{
        height:2rem;
        width:4rem;
        background-color:rgb(26, 26, 178);
        color:white;
        border:5px blue;
        border-radius:2px;
        cursor:pointer;
      }
      button:focus{
         transform:scale(0.95);
         box-shadow:0 0 10px rgba(47, 1, 183, 0.355)
      }
      p{
         margin-top:3rem;
          color:black;
          font-size:1.2rem;
          text-align:center;
          text-decoration:solid;
      }
      .result{
        display: flex;
        gap:1rem;
        margin-top:1.5rem;
        color:rgb(21, 21, 128);
        font-size:1.2rem;
      }
      a{
        
          color:red;
      }
      @media (min-width: 769px) and (max-width: 900px) {
           
           .container{
             margin-top:10rem;
             width:70vw;
           }
           h1{
              font-size:3rem;
          }
           label{
            font-size:1.6rem;
          } 
          input{
             width:25rem;
             height:3rem;
          }
          button{
             height:3rem;
             width:5rem;
          }
          p{
              font-size:1.4rem;
          } 
          .result,a{
            font-size:1.6rem;
          }
      }
      @media (max-width:768px)
      {
        
          .container{
               width:90vw;
               height:80vh;
          }
          h1{
              font-size:1.4rem;
          }
          label{
            font-size:1.1rem;
          }
          input{
             width:12rem;
          }
        
          p{
              font-size:1rem;
          }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>URL Shortening Tool</h1>
      <form action="/url/short" method="post">
        <label for="floatingInput">Paste the URL to be shortened</label>
        <div class="section">
          <input type="text" id="floatingInput" name="LongUrl" required />
          <button>Submit</button>
        </div>
      </form>
     
      <% if(ShortUrl) { %>
      <div class="result">
        Your Link is :
        <a href="http://localhost:8080/url/<%= ShortUrl %>"><%= ShortUrl %></a>
      </div>
      <% } %>
      <p>
        ShortURL is a free tool to shorten URLs and generate short links URL
        shortener allows to create a shortened link making it easy to share
      </p>
    </div>
  </body>
</html>
